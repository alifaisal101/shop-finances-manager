import { IonPage } from '@ionic/react';
import Header from '../../../global/Header/Header';

const PrintBarcode = () => {
  return (
    <IonPage>
      <Header title={'طباعة باركود'}></Header>
    </IonPage>
  );
};

export default PrintBarcode;
