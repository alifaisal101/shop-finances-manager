import { IonPage } from '@ionic/react';
import Header from '../../../global/Header/Header';

const AppSettings = () => {
  return (
    <IonPage>
      <Header title={'إعدادات النظام'}></Header>
    </IonPage>
  );
};

export default AppSettings;
