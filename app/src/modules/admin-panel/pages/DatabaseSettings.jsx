import { IonPage } from '@ionic/react';
import Header from '../../../global/Header/Header';

const DatabaseSettings = () => {
  return (
    <IonPage>
      <Header title={'إعدادات قاعدة البيانات'}></Header>
    </IonPage>
  );
};

export default DatabaseSettings;
