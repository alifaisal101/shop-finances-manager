import { IonPage } from '@ionic/react';
import Header from '../../../global/Header/Header';

const Roles = () => {
  return (
    <IonPage>
      <Header title={'الأدوار'}></Header>
    </IonPage>
  );
};

export default Roles;
